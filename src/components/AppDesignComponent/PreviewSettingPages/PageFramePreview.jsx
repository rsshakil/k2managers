import React from "react";

const PageFramePreview = ({ pageId,data}) => {
    return (
        <div className={`w-full flex justify-center items-center`}></div>
    );
};
export default PageFramePreview
