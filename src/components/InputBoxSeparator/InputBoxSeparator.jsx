import React from "react";

const InputBoxSeparator = ({cProperty}) => {
    return (
        <div className={`flex flex-col ${cProperty} border-dotted border`}></div>
    );
};
export default InputBoxSeparator
